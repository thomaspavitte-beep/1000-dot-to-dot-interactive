# Book covers

Drop book cover images here (jpg/png/webp, portrait orientation — they display
at roughly 44×60px, so anything ~300px tall is plenty). Reference them from
`art/manifest.js` via the `cover` field, e.g. `covers/cityscapes.jpg`.

If a referenced file doesn't exist yet, the site shows a lettered placeholder
tile instead — nothing breaks.
